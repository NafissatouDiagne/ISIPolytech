spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3308/TP_commandes_isi}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${SERVER_PORT:8080}